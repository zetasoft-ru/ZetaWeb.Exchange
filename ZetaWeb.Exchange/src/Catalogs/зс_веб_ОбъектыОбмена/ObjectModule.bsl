
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.ТЧ Тогда
		
		ПроверяемыеРеквизиты.Добавить("РодительскийОбъектОбмена");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		
		Возврат;
		
	КонецЕсли;
	
	Если НЕ ЭтоГруппа Тогда
	
		Если ПустаяСтрока(ИмяМетаданныхНаСайте) Тогда
			
			Отказ = Истина;
			
			зс_веб_ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не заполнено имя метаданных для сайта");
			
		КонецЕсли;
		
		Если ИмяПланаОбмена = Перечисления.зс_веб_ИменаПлановОбмена.ПустаяСсылка() Тогда
			
			Отказ = Истина;
			
			зс_веб_ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не указан план обмена");
			
		КонецЕсли;
		
		Если ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.Спр Тогда
			
			ТипОбъектаСтрокой = "Спр";
			
		ИначеЕсли ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.Док Тогда
			
			ТипОбъектаСтрокой = "Док";
			
		ИначеЕсли ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.ПВХ Тогда
			
			ТипОбъектаСтрокой = "ПВХ";
			
		ИначеЕсли ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.РегНак Тогда
			
			ТипОбъектаСтрокой = "РегНак";
		
		ИначеЕсли ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.РегСв Тогда
			
			ТипОбъектаСтрокой = "РегСв";
			
		ИначеЕсли ТипОбъекта = Перечисления.ЗетаWEBТипыОбъектовКонфигурации.ТЧ Тогда
			
			ТипОбъектаСтрокой = "ТЧ";
			
		Иначе
			
			ТипОбъектаСтрокой = "";
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры
